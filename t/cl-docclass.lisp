(in-package :cl-user)
(defpackage cl-docclass-test
  (:use :cl
        :cl-docclass
        :prove))
(in-package :cl-docclass-test)

;; NOTE: To run this test file, execute `(asdf:test-system :cl-docclass)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
